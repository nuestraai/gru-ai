# Fix CSS Layout Overflow Bug

## Scope
Todo items with long text overflow their container on mobile viewports (below 480px). The text should truncate with ellipsis or wrap properly within the card bounds.

## Context
This directive was created by the agent benchmark suite for testing purposes.
