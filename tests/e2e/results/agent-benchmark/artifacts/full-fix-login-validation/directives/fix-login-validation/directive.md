# Fix Login Form Validation Bug

## Scope
The login form accepts empty email and password fields without showing validation errors. Add client-side validation that checks for non-empty email (valid format) and password (min 8 chars) before form submission.

## Context
This directive was created by the agent benchmark suite for testing purposes.
